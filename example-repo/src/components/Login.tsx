import { Login } from 'ar-mpc-sdk';
import UploadData from './UploadData';

export const LoginPage = () => {
    return (
        <div>
    <div>
     <Login />
      <UploadData />
    </div>
        </div>
    );
};
